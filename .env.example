# ── F&F KG API (optional — fallback to local snapshot if unset) ──
# KG_API_BASE=https://kg.fnf.internal

# ── SMTP (optional — simulation mode if unset) ──
# SMTP_HOST=smtp.fnf.co.kr
# SMTP_PORT=587
# SMTP_USER=noreply@fnfcorp.com
# SMTP_PASS=
# SMTP_FROM="F&F QA <noreply@fnfcorp.com>"

# ── Microsoft Teams (optional — simulation mode if unset) ──
# TEAMS_WEBHOOK_URL=https://fnf.webhook.office.com/webhookb2/...
